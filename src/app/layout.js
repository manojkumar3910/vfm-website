import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "VForce Tourism & Travels - Explore India",
  description: "Your trusted partner for unforgettable travel experiences across India. Book your dream vacation with VForce Tourism & Travels today!",
  keywords: "travel, tourism, India, tours, vacation, holiday, tour packages, travel agency",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
